import { MD3LightTheme as DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#6200EE",
    accent: "#03DAC5",
    background: "#F5F5F5",
    surface: "#FFFFFF",
    text: "#000000",
  },
};

export default theme;
